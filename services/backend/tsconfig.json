{
    "extends": "../../tsconfig.json",
    "compilerOptions": {
        "outDir": "build",

        "baseUrl": "./source",
        "paths": {
            "@config": ["config"],
            "@domain/*": ["domain/*"],
            "@auth": ["shared/auth"],
            "@logs": ["shared/logs"],
            "@validators": ["shared/validators"],
            "@testing": ["shared/testing"],
        },
    },
    "exclude": [
        "node_modules",
        "build"
    ]
}
