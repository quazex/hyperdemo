{
    "extends": "../../tsconfig.json",
    "compilerOptions": {
        "outDir": "build",

        "baseUrl": "./source",
        "paths": {
            "@config": ["config"],
            "@models/*": ["models/*"],
            "@validators": ["shared/validators"],
        }
    },
    "exclude": [
        "node_modules",
        "build"
    ]
}
