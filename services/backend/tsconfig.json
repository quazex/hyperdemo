{
    "extends": "../../tsconfig.json",
    "compilerOptions": {
        "outDir": "build",

        "baseUrl": "./source",
        "paths": {
            "@config": ["config"],
            "@domain/*": ["domain/*"],
            "@validators": ["shared/validators"],
            "@testing": ["shared/testing"],
        }
    },
    "exclude": [
        "node_modules",
        "build"
    ]
}
